
/**
 * string/text string
 *      => "", '', ``(backtick)
 * number
 *      => < 0 >
 * date
 *      => Y-M-D
 * boolean
 *      => true, false
 * null
 *      => null(pure null), "", '', `` (ALL THREE ARE string null)
 * array
 *      => array is a collection of data, []
 * object
 *      => instantiation of a class, {}
 * JSON
 *      => {}
* Undefined
        => 'value is not assigned'
 * NaN
        => Not a Number.
 */

// array declaration
let user = [];
let user_1 = new Array();

// object data declaration
let obj = {};

// JavaScript is a weakly typed programming language because you do not have to specify the variable type in advance.
let var_dec;        // undefined

console.log("John" + " " + "Doe");
console.log(2 + 3 + "5");
